(function(){const isPullRequest = location.href.match(/pull-requests\/\d*/);if (!isPullRequest) return;const copyToClipboard = (str) => {const textarea = document.createElement('textarea');textarea.value = str;document.body.appendChild(textarea);textarea.select();document.execCommand('copy');document.body.removeChild(textarea);};const prNumber = isPullRequest[0].split('/')[1];const gitCloneCommand = `git fetch origin refs/pull-requests/${prNumber}/from:review-pr-${prNumber} && git checkout review-pr-${prNumber}`;copyToClipboard(gitCloneCommand)})()